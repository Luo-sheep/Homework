#include <stdio.h>
#include <stdlib.h> 
int main()
{
    int a,b,c,m;
    if (a>=b & a>=c) 
    {
        
        printf("%d %d %d",a,b,c) ;
    }
        
    else
        if (b>=a & a>=c)
        ;

    return 0;
}
